# SMTP configuration (required for real emails)
# For Gmail use smtp.gmail.com with an App Password (recommended when 2FA is enabled)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false          # false for 587, true for 465
EMAIL_USER=your@gmail.com   # the SMTP auth user (usually the same Gmail address)
EMAIL_PASS=your-app-password

# The admin/receiver email where contact messages should be sent
ADMIN_EMAIL=kaundalanshul725@gmail.com

# Optional: skip DB/Cloudinary for quick local testing
# SKIP_DB=true
# SKIP_CLOUDINARY=true
